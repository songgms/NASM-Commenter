# Changelog

本项目的所有显著变更将记录在此文件中。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循 [SemVer](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.5.0] - 2026-08-29

### Added

- 自动补全：`section`/`segment` 参数补全段名（.text/.data/.bss/.rodata/.rdata），`bits` 参数补全位数（16/32/64）

### Changed

- 虚拟注释按文档版本缓存，编辑/滚动触发的 Inlay Hint 请求不再重复全量计算
- 切换虚拟注释开关后立即请求客户端刷新（无需再编辑一次才生效）

## [0.4.0] - 2026-08-29

### Added

- **虚拟注释预览（默认开启）**：注释以 Inlay Hint 幽灵文字在行尾展示，不修改文档；悬停显示指令语义；`NASM Commenter: 切换虚拟注释预览` 命令开关
- **去掉所有注释**：新命令清除文件内全部注释（含手写注释，模态确认；整行注释删行、行尾注释剥离、字符串内 `;` 不受影响）；灯泡菜单同步提供入口

### Changed

- `applyEditsToText` 升级为跨行编辑语义（与客户端 WorkspaceEdit.replace 一致）

## [0.3.0] - 2026-08-29

### Changed (不兼容)

- 自动注释不再携带 `[nasm-commenter]` 标记，注释形如 `; 将立即数 1 加载到 rax`
- 注释内标点统一为英文半角（`（）：，、` → `(): ,`）
- 「移除自动注释」改为按规则引擎当前生成结果匹配移除；`旧注释 / 新注释` 追加形式只剥离追加部分；LLM 增强的注释不参与移除
- 状态栏覆盖率改为按规则引擎匹配统计（打开/注释后展示「引擎可注释行 / 总行数」）

### Added

- `nasm-commenter.marker` 配置：设置标记前缀（如 `[nasm-commenter] `）可恢复标记模式与精确移除

## [0.2.0] - 2026-08-29

### Added

- 自动补全：命令位置补全指令名（附语义说明），操作数位置补全寄存器与文档标签（跳转指令优先标签）
- 诊断推送：未知指令（Hint）、跳转到未定义标签（Warning），打开/编辑时实时更新 Problems 面板
- 状态栏覆盖度：打开文件即显示注释覆盖率，注释请求后即时刷新
- 扩展图标：脚本生成的汇编分号标识（`assets/icon.png`，零依赖 PNG 生成器）
- CodeAction「为当前函数生成块注释」；操作数补全包含 `%define` 宏常量名
- LLM 适配器请求延迟/失败/缓存命中日志
- README 目录结构、安装章节与 CI 徽章；CI 打包上传 VSIX artifact

### Fixed

- 带 UTF-8 BOM 的文件首行可正常解析与注释（token 位置保持正确）
- 诊断推送 300ms 防抖；补全/诊断共享跳转助记符常量

- 寄存器追踪：写宽寄存器（如 rax）后失效全部子寄存器（eax/ax/al），消除基于残留旧值的错误注释
- 栈帧信息：epilogue（leave）后正确释放，不再泄漏到后续行
- 悬停系统调用号（docs/07 §3.5.2）：`mov rax, 60` 的立即数显示 `exit(60)` 与参数/返回说明
- 栈帧提示接线（docs/03 §12）：`[rbp-N]` →（局部变量 N）、`[rbp+N]` →（函数参数 N）

## [0.1.0] - 2026-08-29

### Added

- LSP 双进程架构：client（VSCode 扩展宿主）+ server（独立语言服务器）
- 行级词法分析器：Token 识别、操作数解析、内存寻址、错误恢复
- 数据驱动知识库：指令语义模板（中英双语）、寄存器约定、多指令模式、系统调用表
- 注释引擎：模板匹配、条件片段、置信度、`[nasm-commenter]` 标记、去重保护
- 上下文追踪：基本块划分、寄存器常量传播、栈帧识别、系统调用参数回溯、ABI 自动检测
- 模式匹配：函数序言/结尾、strlen、write/exit 系统调用、循环等惯用模式
- LSP 能力：Hover 指令/寄存器提示、CodeAction、自定义注释请求
- 6 个命令：注释文件/选区/行/函数、移除自动注释、切换行内/上方样式
- 可选 LLM 增强：OpenAI 兼容 + Ollama 适配器，默认关闭，失败静默回退规则结果
- 三层测试：单元 / 集成（fixtures）/ 快照，Vitest 驱动
